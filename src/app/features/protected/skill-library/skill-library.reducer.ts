import { SkillLibraryAction } from './skill-library.actions';

const INITIAL_STATE: Object = {};

export const skillLibraryReducer = (state: Object = INITIAL_STATE,
                                 action: SkillLibraryAction): Object => {
	return state;
};
