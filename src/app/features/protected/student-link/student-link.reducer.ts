import { StudentLinkAction } from './student-link.actions';

const INITIAL_STATE: Object = {};

export const studentLinkReducer = (state: Object = INITIAL_STATE,
                                 action: StudentLinkAction): Object => {
	return state;
};
