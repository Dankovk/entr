import { StudentAssignmentDetailAction } from './student-assignment.actions';

const INITIAL_STATE: Object = {};

export const studentAssignmentDetailReducer = (state: Object = INITIAL_STATE,
                                 action: StudentAssignmentDetailAction): Object => {
	return state;
};
